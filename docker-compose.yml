version: "3.8"
services:
  go-cron:
    image: go-cron
    build: .
    working_dir: /app
    volumes:
      - ./:/app
      - jobs_data:/app/jobs.json
    ports:
      - "8080:8080"
    command: air -c .air.toml
    environment:
      - AIR_PORT=8080

volumes:
  jobs_data: 